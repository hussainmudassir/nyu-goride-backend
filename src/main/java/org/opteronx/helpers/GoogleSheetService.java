package org.opteronx.helpers;

public class GoogleSheetService {

    //write functionality to read googlesheets and process it as a JSON.
    //It might require us to login first, in that case we can use the static file and then read in the runtime.

    public void readGoogleSheet(String sheetName) {
        //TODO

    }
}
